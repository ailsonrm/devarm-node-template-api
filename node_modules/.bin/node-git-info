#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-git-info/bin/node-git-info" "$@"
  ret=$?
else 
  node  "$basedir/../node-git-info/bin/node-git-info" "$@"
  ret=$?
fi
exit $ret
